{
  "name": "canal-courier",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client",
    "build:single": "npm run build:single --workspace=client",
    "extract-assets": "node scripts/extract-assets.mjs",
    "extract-ui-art": "node scripts/extract-assets.mjs --ui-only"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
